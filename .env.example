# Backend base URL — consumed by scripts/gen-api.mjs at codegen time.
BACKEND_URL=http://localhost:8000

# Google Stitch MCP (Cursor ~/.cursor/mcp.json) — optional, for UI generation workflow.
# STITCH_API_KEY=
